<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Ink Touch - Large Format Printing</title>
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <meta content="" name="keywords" />
    <meta content="" name="description" />

    <!-- Favicon -->
    <link href="../../img/ink.jpg" rel="icon" />

    <!-- Google Web Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Heebo:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />

    <!-- Icon Font Stylesheet -->
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.0/css/all.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css"
      rel="stylesheet"
    />

    <!-- Libraries Stylesheet -->
    <link
      href="../../lib/owlcarousel/assets/owl.carousel.min.css"
      rel="stylesheet"
    />
    <link
      href="../../lib/tempusdominus/css/tempusdominus-bootstrap-4.min.css"
      rel="stylesheet"
    />

    <!-- Customized Bootstrap Stylesheet -->
    <link href="../../css/bootstrap.min.css" rel="stylesheet" />

    <!-- Template Stylesheet -->
    <link href="../../css/style.css" rel="stylesheet" />
    <link href="../../css/viewBtn.css" rel="stylesheet" />
  </head>

  <body>
    <div class="container-xxl position-relative bg-white d-flex p-0">
      <%- include (`../partials/adminHeader`); -%>

      <!-- Content Start -->
      <div class="content">
        <!-- Navbar Start -->
        <%- include (`../partials/adminNav`); -%>
        <!-- Navbar End -->

        <!-- Recent Sales Start -->
        <div class="container-fluid pt-4 px-4">
          <div class="bg-light text-center rounded p-4">
            <div class="d-flex align-items-center justify-content-between mb-4">
              <h6 class="mb-0">All Wastage</h6>
            </div>
            <div class="table-responsive">
              <table
                class="table text-start align-middle table-bordered table-hover mb-0"
              >
                <thead>
                  <tr class="text-dark">
                    <th scope="col">Date</th>
                    <th scope="col">Ref</th>
                    <th scope="col">User</th>
                    <th scope="col">Category</th>
                    <th scope="col">Size</th>
                    <th scope="col">Material</th>
                    <th scope="col">Quantity</th>
                    <th scope="col">narration</th>
                    <th scope="col">Status</th>
                  </tr>
                </thead>
                <tbody>
                  <% allWastet.forEach(waste => { %>
                    <tr>
                      <td>
                        <%= new Date(waste.createdAt).toLocaleString('en-NG', {
                        timeZone: 'Africa/Lagos' }).replace(',', '') %>
                      </td>
                      <td><%= waste.ref %></td>
                      <td><%= waste.operator %></td>
                      <td><%= waste.category %></td>
                      <td><%= waste.size %></td>
                      <td><%= waste.material %></td>
                      <td><%= waste.waste_quantity %></td>
                      <td><%= waste.narration%></td>
                      <td>
                        <select class="form-select account-status-select" data-waste-id="<%= waste._id %>">
                          <option value="Pending" <%= waste.waste_status === 'Pending' ? 'selected' : '' %>>Pending</option>
                          <option value="Approved" <%= waste.waste_status === 'Approved' ? 'selected' : '' %>>Approved</option>
                          <option value="Rejected" <%= waste.waste_status === 'Rejected' ? 'selected' : '' %>>Rejected</option>
                      </select>
                      </td>
                    </tr>
                    <% }) %>
                </tbody>
              </table>
              <div class="pagination">
                <% for (let i=1; i <=totalPages; i++) { %>
                <a
                  href="/admin/all-wastage?page=<%= i %>"
                  class="<%= currentPage === i ? 'active' : '' %>"
                >
                  <%= i %>
                </a>
                <% } %>
              </div>
            </div>
          </div>
        </div>
        <!-- Recent Sales End -->

        <!-- Footer Start -->
        <%- include (`../partials/adminFooter`); -%>
        <!-- Footer End -->
      </div>
      <!-- Content End -->

      <!-- Back to Top -->
      <a href="#" class="btn btn-lg btn-primary btn-lg-square back-to-top"
        ><i class="bi bi-arrow-up"></i
      ></a>
    </div>
    <script src="../../mail/waste-status.js"></script>
  </body>
</html>
